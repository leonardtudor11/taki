httpx>=0.27
pydantic>=2.9
python-dotenv>=1.0
tenacity>=9.0
pytest>=8.0
# Vertex AI via ADC (preferred LLM path). Lazy-imported in services/llm.py.
google-genai>=2.3.0
# Multi-agent graph runtime — explicit parallel dept fan-out + event streaming.
langgraph>=0.2,<0.3
# Local backend for live cascade runs from the dashboard (server.py).
flask>=3.0,<4
flask-cors>=4,<6
